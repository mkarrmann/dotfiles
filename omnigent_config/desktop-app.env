# First-install pin from https://omnigent.ai/download/linux-deb.
# Update the version, versioned asset URL, and checksum together.
# When bumping, re-check the launcher override in omnigent-desktop-electron.desktop
# (still needed? still matching the package entry?); see the HACK note there.
# bin-linux/startup-windows needs >= 0.13.0: that release added the
# Server > New Window accelerator the per-workspace Omnigent windows depend on.
OMNIGENT_DESKTOP_APP_VERSION=0.14.0
OMNIGENT_DESKTOP_APP_URL=https://diksk5m140cfbma7.public.blob.vercel-storage.com/linux/omnigent-desktop-electron_0.14.0_amd64.deb
OMNIGENT_DESKTOP_APP_SHA256=8246638e15bafbe2db3e4c2d5114027f4ac2617a66f43d0a921f4673df9a955c
