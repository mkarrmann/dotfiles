{
  "description": "Subscribe the current Omnigent session to review, CI-failure, AI-review, and green-CI notifications for submitted or explicitly selected Phabricator diffs.",
  "agents": ["claude"],
  "mcpServers": {
    "diff_watch": {
      "type": "stdio",
      "command": "~/dotfiles/bin/omnigent-diff-watch-mcp",
      "args": ["--native-claude"]
    }
  }
}
