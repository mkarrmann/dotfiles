{
  "description": "Query Scuba datasets directly. Get table schemas and run SQL queries for data analysis and debugging.",
  "mcpServers": {
    "scuba": {
      "type": "stdio",
      "command": "~/bin/scuba-mcp-launcher",
      "env": {
        "SCUBA_MCP_DEV": "${SCUBA_MCP_DEV}"
      }
    }
  }
}
