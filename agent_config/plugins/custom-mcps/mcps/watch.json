{
  "description": "Wake an Omnigent session when a command reports a change: job completion, deployment status, file changes, or another custom event. subscribe, status, and unsubscribe manage generic watches; diff_subscribe, diff_status, and diff_unsubscribe handle Phabricator watches at work. Each takes the session_id to wake.",
  "profiles": [
    "desktop",
    "work"
  ],
  "mcpServers": {
    "watch": {
      "type": "stdio",
      "command": "~/dotfiles/bin/omnigent-watch-mcp"
    }
  }
}
