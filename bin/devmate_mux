#!/bin/bash
# cd into fbsource so the real devmate_mux finds .hg/reponame
# and skips the broken `scsc lookup` (fails with machine identity on devvms).
cd "$HOME/main/fbsource" 2>/dev/null
exec /usr/local/bin/devmate_mux "$@"
